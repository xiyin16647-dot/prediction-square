# 复制本文件为 .env 后填入真实值

# Neon Postgres 连接串（在 neon.tech 控制台拿）
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# JWT 签名密钥（生产环境必须改成随机长字符串，可用 `openssl rand -hex 32` 生成）
JWT_SECRET="dev-only-secret-change-in-production"
